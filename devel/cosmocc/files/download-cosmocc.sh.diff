--- build/download-cosmocc.sh.orig	2025-02-08 17:57:03.144225923 -0500
+++ build/download-cosmocc.sh	2025-02-08 17:57:17.503831086 -0500
@@ -99,3 +99,8 @@
 # commit output directory
 cd "${OLDPWD}" || die
 mv "${OUTPUT_TMP}" "${OUTPUT_DIR}" || die
+
+# update current symlink
+BASE=$(basename "${OUTPUT_DIR}")
+DIR=$(dirname "${OUTPUT_DIR}")
+ln -sfn "$BASE" "$DIR/current"
